I want to create a new article which is meant to be posted in ethresear.ch The idea or goal of the article is to mainly discuss and paste some thoughts on my head around whether we can actually marry together scalability in terms of, uh, database and state management. And at the same time, throughput of the chain with new architectural solutions for clients and taking into account how the client ecosystem is likely gonna change in the future. So for that, we will divide the article into sections. One, which is going to be related to more... like, the vision that I have for how clients will look like and what clients we will have once we ship ZKEVM and in the second place, we will have another section, which is actually gonna tackle, uh, this new type of architecture, uh, where... or new type of, uh, call it, idea or protocol change, whatever, where we actually marry together throughput in, um, state management on a much better way for the new types of nodes or the nodes that will remain after ZKEVM takes over.

So on the first section, there's clearly there's clearly gonna be a change, uh, after not optional proofs, but mandatory proofs land in Ethereum mainnet. Basically, there's no incentive whatsoever to hold state anymore by any client... or, sorry, or by any validator, let's say, because at the end, they are profit driven, and they can shave significantly the hardware they have and that they are running. And in return, they have no penalty at all because they can do stateless and executionless verification with zed k e v m proofs. So what that means is that we should mainly expect a big chunk of the validators, uh, to migrate into this new type of, uh, stateless nodes or zed k e v m nodes. And once that happens, then the question is what types of clients are left? Well, clearly, RPCs are going nowhere. They are still needed or more needed than they even were before. So there's no change for them besides they are gonna get more utilization, and they will mainly need to scale and improve. because zed k e v m's allow us to mainly increase throughput since verification is execution less. And, therefore, when really limited by the wire, so basically propagation, what that means is we should expect gas limits to be raised significantly because at the end, builders on its majority are very complex entities capable of crafting much, much bigger blocks than they craft today. And so what are these goals is for RPCs, they will need to get much better at faster re execution because they will indeed need it, at least, if they want to be an archive and certify car... icon... and serve archive data. And on top of that, they will also need to get smarter on its state data management, which is what will tie this section to a section two. Something similar to what Erigon the Ethereum client does, where we need to start thinking about archive nodes or RPC nodes as multi machine nodes or, at the very least, multi disk machines or entities. Because otherwise, state... gas limit goes up. State starts to starts to crawl up in size very quickly. The more utilization we give to users, the more we make state grow, and clients need to be able to support that somehow. They need to adapt to this new reality. Then we move to full notes, which to my view are essentially just gonna disappear, basically. They are gonna be this rare entity, which has close to no usage, except if you want to be a nonsophisticated builder or a solo builder. There's no real reason to run a full note for absolutely anything in the sense a full note will do reexecution when it's basically not needed. It will serve state when it's actually not needed either. And on top of all these things, full note is not meant to serve state professionally, let's say. So I see very rare usage for full notes in the future reality of Ethereum working with CK Proofs. Then we move to one type of client that I think is gonna proliferate a lot more, which is partial stateful clients. When state size heavily increases, lots of dApps are... that are depend today on RPCs are gonna see costs of RPCs increased due to this needed changes in optimizations and hardware increases. And so a really good alternative to depending on these third parties is to basically run a partial stateful node, which allows you to only gather the state that you're interested on and still be able to craft proofs for it. These, to me, look like the replacement notes to full notes in the sense they are still useful and they still allow you to do some extra tasks that are just being a validator. But as opposed to full notes, they don't need to actually hold the entire state. And these will come in handy for things like fossil, the Ethereum upgrade, that we're expecting to get in Hegota. So for fossil, running a partial state for nonvikes quite some sense because you get a much reduced, uh, state size. And on top of that, you have all the account data, which is basically what you need in order to be an include there. includer. I don't foresee any new types of nodes besides that. What I imagine is that client teams, except Errigo, and, of course, will slowly try to pivot to serve particular use cases for nodes on a much, much better way. But I really don't see a world if we do mandatory proofs where we have full clients or we have the types of nodes that we have today and teams work on the same things they work on today. So that essentially leaves a chain thought that moves us towards how can we think about this future that comes with mandatory proofs. Well, one could say that the only thing we have to worry about is the preservation of the chain. Right? So, basically, make sure that we tackle state growth, which is the... probably the last or one of the latest problems that we need to fix prior to being a lot more comfortable moving towards any kind of ossification or whatever. And so On this side, there are two very important things to highlight. One is RPC nodes and archive nodes are going to be more relevant in the future because a lot less people is gonna be holding state. And so most of the Ethereum resilience will be on their hands as it is mostly today anyways. The problem is you will not have a backup of a big number of full nodes. I think it's about thirteen thousand today. Having the full state and being ready to serve it to you. So on the same way we have portal, and Errigon has torrenting, we will need a distributed history slash state solution that essentially will help to mitigate any sort of issues related to state unavailability. But it is really important to highlight here that such state solution is not meant to be fast. We don't need to serve tip of the chain data. Right? Because RPCs are specifically designed for that. What we want to make sure is that users of Ethereum can always access their data and make a transaction related to their data even if that data is called. And so in order to warranty that, we need such a decentralized third serving solution, call it torrenting, call it whatever you want, something along the lines of what Eragon does. but we don't want it to be extremely complicated. Right? We want it to be as simple as possible. In fact, one can argue that the only way to make something like that work and land in Ethereum is to make it as simple as possible. The problem is, as said, clients today accept the ferry gun are not really wired on a way in which this can be made easily a change. Luckily, the stateless consensus team in the EF has started pushing for an EIP for Hegota called eight one eight eight. What this EIP does is essentially marking when data was last written. And so what we get with that is we can basically say whether data is hot or cold. That is a completely nonharmful change in the sense all of this new data will only change the RLP encoding of accounts, but it will certainly... or slots, but it will certainly not change massively the database in size. So state dBs are expected to go up by one or two gigabytes as much. There are other ideas on how to do this, and there's a lot of things still to understand, like what's the time period where we consider data being... moving from hot to cold. but this opens the door to one of the most critical things, and I'm recovering the the the argumentative part of of the section number one, which is how can we take that to our advantage to make the change better. So so far, we have agreed that we need, uh, the centralized state serving solution. That doesn't aim to be fast. It only aims to be resilient. It distributes state across wallets, partial stateful notes, or any type of, uh, client or entity that is running or interacting with the chain via torrenting. And so on this... on the second part, now that we have the the resilience layer, let's say, what what we actually need is to give more throughput to the network and scale it up such that it actually becomes usable by a lot more people at the same time. And it can adopt or digest or embrace the financial system of the world. And so a good way to do it is to take into consideration one very important thing, which is that eighty percent of the data or eighty percent of the state written to Ethereum goes cold in thirty days. What this means is there's an eighty percent chance that in every block, we are writing data that will go stale in thirty days or less. And so We can use that to our advantage. One could say, On the same way, we are doing repricings in Glumsterdam. We could reprice state taxes again, but this time, based on how stale or cold the data is. What was... what will this give us? Well, first of all, if we can make this kind of repricing happen, clients will definitely be able to adopt new architectures much more similar to Errigon's, where they can hold most of the hot state on their main databases. They could even be memory depending on how much hot state that we have in the time period that we define, but let's just assume that they they have much, much more smaller databases where they hold most of the state. And all of the call state is just moved to either flat files, different disks, which are cheaper like satis, DDs, or something like that. It can be moved, uh, to different machines and connected via ten gigabit per second Ethernet. You name it. What this allows... what this architectural change for clients, and especially RPCs and builders allows, is that Since we no longer have the classical full node as the main entity or target for our upgrades, we move towards a paradigm where we will help the network scale the throughput by allowing both builders and RPCs to now have a more in protocol aligned architecture where hot data is always served much faster, and they can have the warranty that the worst cases for cold data access will be not punished, but at least significantly apprised such that no one can craft a block that touches insane amounts of very old state and just completely crashes the network or the node or similar. Notice that you can only crash two entities when we move to mandatory proofs in Ethereum. You can either crash the builder or you can crash the RPCs, but there's no more full nodes to crash. Because if the builder is able to produce the block, all of the verifiers... so, basically, all of the attesters or validators will be able to validate it. So The network only stalls if the builder is not able to produce a block and a proof. So if we allow the builder to be faster by giving him the warranty that there will be significantly higher prices for call data access such that they can redo their databases accounting for that, then it's a much stronger signal in warranty for them that they can heavily optimize on hot state access, and they can make a much more complex and performance solutions for state access. And the same applies to RPCs. RPCs, especially the ones that want to Winterseer one to surf archive data. They need to reexecute no matter what, and they need to reexecute at a pace where they can keep up with the chain. Right? Block access lists and all these kind of stuff, it's it's certainly useful. But the core here is we need to give them a fast database to update hot state as fast as possible. So now we have wired EIP eight one eight eight and the future of the clients that we will expect to see in a few years in Ethereum. So now how do we marry those two, and we optimize this? What we have been discussing on this last part. And one of the things that we would like to do when we have discussed with Toni Wahrstätter. But so far, haven't been able to yet figure out exactly is if we significantly upprice call data to a point where it is... I don't know. We can just assume that it's in a satire SSD, so very cheap hardware. The problem that we get from there is that the upprice of this data is potentially going to be extremely high to the point where block utilization might fall insanely. Imagine that someone needs to, I don't know, do something with an old wallet where it has fifty USDC. Well, depending on the repricing, it it might just be worthless to try recovering this account because it will it will cost you more money to actually create a new one. This is, of course, an exaggeration, but it's something that we need to be really careful about. Because without knowing, we might just be forgiving access to a lot of old state when when we shouldn't. So here, we have the obvious solution, which is, well, let's allow the user or the transactor provide provide the proof. So, basically, the user sends a transaction. The transaction has a proof attached to it, a storage proof. And this storage proof prove the existence of all of the values that are in the cold state such that the node doesn't even need to fetch them. Right? The node can just verify the state route, verify the proof, get the values, and do the execution. And that applies both for the builder and for the RPC. The sad part is that it doesn't. it applies for the builder, but the problem is if we want to get a discount, if we want to give a discount to the person or the entity that has submitted this proof, well, we actually need to prove that we got a proof for that, which is surprisingly stupid, but surprisingly complex to fix. At least naively. If the consensus needs to agree on whether cold access data should be discounted or not based on a proof that the transaction had attached and the builder received, Well, there's no more alternatives than just attaching this proof to the transaction and to the block. The problem now becomes that if a lot of transactions touch cold state within the same block, all of these proofs will need to be included in the block. And then we'll need to be propagated by the builder. And so we can be reaching, like, massive size proofs and massive size blocks because the block is big. The block access list is big, and the proofs are certainly big, especially in a... in an MPT. And so this is where I am now quite certain that this is the correct path to go towards, but still unsure about the particular solution that I've named. It definitely has to be something along these lines, but it's not obvious to me that this will work or there's some remedy to it, or we need to just try to reinvent the way.
